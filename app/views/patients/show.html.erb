<h1><%= @patient.name%></h1>
<ul>
  <% @patient.appointments.each do |appt| %>
    <li>
      <ul><%= "#{appt.date} at #{appt.time}"%></ul><br>
      <ul><%= link_to appt.doctor.name, doctor_path(appt.doctor)%></ul><br>
    </li><br>
  <% end %>
</ul>
