<h1>Patients</h1>
<ul>
  <% @patients.each do |patient| %>
    <li><%= link_to "Name: #{patient.name}", patient_path(patient)%><br>
       <%= "Number of Appointments: #{patient.appointments.count}"%></li><br>
  <% end %>
</ul>
