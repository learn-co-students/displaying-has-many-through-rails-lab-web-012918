<% @patient.appointments.each do |app| %>
  <%= app.appointment_datetime.strftime("%B %d, %Y")%> at <%= app.appointment_datetime.strftime("%H:%M") %>
  <%= link_to app.doctor.name, doctor_path(app.doctor.id) %>
<% end %>
