<h1><%= @doctor.name%></h1>
<h2><%= @doctor.department%></h2>
<ul>
  <% @doctor.appointments.each do |appt| %>
    <li>
      <ul><%= "#{appt.date} at #{appt.time}"%></ul>
      <ul><%= link_to appt.patient.name, patient_path(appt.patient) %></ul>
    </li><br>
  <% end %>
</ul>
