<h1>Doctor <%= @doctor.name %></h1>
<% @appointments.each do |app| %>
  <p><%= app.appointment_datetime.strftime("%B %d, %Y at %H:%M") %></p>
  <p><%= link_to app.patient.name, patient_path(app.patient) %></p>
<% end %>
